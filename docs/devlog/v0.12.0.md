# v0.12.0

- 发布日期: 2026-05-09
- 版本: `0.12.0`

## 新增

- 新增 `CheckboxGroup` 表单分组组件，支持多选、单选、`name` 原生表单提交与 `sx` 样式入口。
- 新增 `Input` 表单输入组件，支持原生 input 属性、`class` 别名与 `sx` 样式入口。

## 优化

- 暂无。

## 修复

- 暂无。

## 文档

- 新增 `CheckboxGroup` 文档页与 Multiple、Single、Form Submit、SX Styling 示例。
- 新增 `Input` 文档页与 Basic、States、SX Styling 示例，并接入表单与输入侧边栏。

## 验证

- `pnpm -s tsc --noEmit --ignoreDeprecations 5.0` 通过。
- `pnpm -s build:docs` 通过。
