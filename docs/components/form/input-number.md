# InputNumber

数字输入组件，基于 `Input` 扩展 `type="number"`，适合数量、金额、阈值等场景。

## Demo

<InputNumberDemo />

## 常用 API

- `min` / `max`: 最小值与最大值。
- `step`: 步进值。
- 继承 `Input` 的 `disabled`、`placeholder`、`sx`、`class`、`className` 等能力。