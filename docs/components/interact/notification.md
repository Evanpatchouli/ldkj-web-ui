# Notification

通知能力组件（V1 使用 `toast` facade 包装），提供 `info/success/warn/error` 快捷调用。

## Demo

<NotificationDemo />

## 常用 API

- `notification.info(message)`
- `notification.success(message)`
- `notification.warn(message)`
- `notification.error(message)`